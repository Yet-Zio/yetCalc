<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".YetCalc"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.YetCalc"
        tools:targetApi="tiramisu">
        <activity
            android:name=".views.YetLibsActivity"
            android:exported="false" />
        <activity
            android:name=".HomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".views.SettingsActivity"
            android:exported="false" />
        <activity
            android:name=".views.HistoryActivity"
            android:exported="false" />
        <activity
            android:name=".views.ProgramCalcActivity"
            android:exported="false"
            android:label="@string/programmer" />
        <activity
            android:name=".views.UnitConvActivity"
            android:exported="false"
            android:label="@string/converter" />
        <activity
            android:name=".views.CalculatorActivity"
            android:exported="false"
            android:label="@string/calculator"></activity>
    </application>

</manifest>